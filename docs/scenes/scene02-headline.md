# Scene 2 — What I built (the headline)

- **Video file:** `scene02-headline.mp4`
- **Face-cam:** optional (off recommended)
- **Target:** ~1.5 min
- **Status:** LOCKED — voice-matched 2026-06-18 (see [VOICE.md](VOICE.md))

## What you display
**Deck slide 1 (Title)** full-screen — open [docs/Research-Assistant-Capstone.pptx](../Research-Assistant-Capstone.pptx) in slideshow mode. Its feature chips ("3 agents · 4 patterns · RAG + web · Evaluated · Cloud Run") and subtitle ARE the headline, so the slide carries the visual while you narrate.

## What you say
_(the `(say "...")` bits are pronunciation cues — don't read them aloud)_

"This is a research assistant built from three agents that work together on a single Gemini (say "JEM-in-eye") 2.5 model. The spine that runs them in order is plain code I wrote, so the flow is deterministic. It uses four ADK (say "A-D-K") communication patterns, and the rubric only asks for two. It does RAG over 15 arXiv (say "ARCHIVE") papers with Vertex (say "VER-teks") embeddings and a FAISS (say "FACE") index, and every claim comes back with a page citation. It also searches the live web through Tavily (say "TAV-ih-lee"), and it decides on its own when to use the papers, the web, or both. I evaluate it with Ragas (say "RAH-guss"), I can trace any run through an in-process plugin and Arize Phoenix (say "uh-RISE FEE-niks"), and it's live on Cloud Run. I'll walk through each piece."

## Delivery notes
- Don't machine-gun the stack names. Slight beat between them. Each one is a scene you're about to expand.
- It's a 30-second "whole thing at a glance." Keep the energy up but unhurried.
- You're reading a labeled list off a slide that shows the same five highlights, so there's nothing to recall cold.
