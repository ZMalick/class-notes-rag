# Secrets
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.python-version

# uv
.uv-cache/

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Project data (raw transcripts stay in EA, not here)
data/
*.db
*.sqlite

# Corpus PDFs — reproducible via scripts/download_corpus.py (manifest.json is tracked)
knowledge_base/*.pdf

# RAG vector index (rebuilt from knowledge_base/ via embedder.py)
faiss_index/
*.faiss

# GCP / service-account credentials — never commit (ADC lives in the user dir)
*-key.json
*credentials*.json
service-account*.json

# Eval outputs
eval_results.json
eval_results.csv
