# Copy this file to `.env` and fill in real values. Never commit `.env`.

# Google Gemini — for synthesis (2.5 Flash) and eval judge (2.5 Pro)
GEMINI_API_KEY=...

# Voyage AI — for voyage-3 embeddings
VOYAGE_API_KEY=pa-...

# Postgres + pgvector
# Local Docker default (matches docker-compose.yml we'll write Day 2):
DATABASE_URL=postgresql://rag:rag@localhost:5432/class_notes
